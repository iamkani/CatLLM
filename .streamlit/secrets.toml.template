# Template for Streamlit secrets (do not commit real keys).

OPENAI_CHAT_MODEL = "gpt-4o-mini"
OPENAI_EMBEDDINGS_MODEL = "text-embedding-3-large"
OPENAI_API_KEY = "<set-in-streamlit-secrets>"
CATLLM_BOOTSTRAP_ROOT_PASSWORD = "<set-strong-password-in-secrets>"
